DESCRIPTION:
  Copy a piece of data from one file to new file.

USAGE:
  cutfile <origfile> <targetfile> [begin_offset [length]]

BUILD:
  WINDOWS:
  > cl cutfile.cpp

  LINUX:
  > g++ cutfile.cpp -o cutfile
